/**
 * Actions for the auth reducer. We'll use these in various functiosn such as logging in, logging out, or
 * updating the the current user's information.
 */
const authActions = {
  login: "LOGIN",
  logout: "LOGOUT",
  updateUser: "UPDATE_USER",
};

export default authActions;
