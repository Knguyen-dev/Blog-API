import { axiosPublic } from "./axios";

/**
 * Makes a request to the backend to send email the user an email verification link.
 * So we need to accept a particular user's ID
 */
function sendVerificationEmail() {}
